// Import jest-dom matchers
import "@testing-library/jest-dom/vitest";

// Optional: Add any global setup/teardown here
// e.g., vi.resetAllMocks() before each test if needed, though isolate: true might handle this
// beforeEach(() => {
//   vi.resetAllMocks();
// });
